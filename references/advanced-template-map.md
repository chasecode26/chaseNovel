# 高阶模板导航

这份文件专门收纳那些**不需要每次都加载**、但在特定场景下很有价值的高级模板。

原则：

- 平时不要默认读
- 只有场景明确命中时再补读
- 这些模板是“加成模块”，不是主流程

## 开书与骨架

- `python scripts/project_bootstrap.py --project "novel_书名"`
  - 新书初始化时直接执行
  - 用来生成目录与基础记忆文件，比手填清单更可靠
- `templates/volume_blueprint.md`
  - 做卷纲、卷级冲突、卷末交接时读
  - 用来避免长篇中盘只会堆章节，不会立卷

- `templates/style_fingerprints.md`
  - 需要补具体叙述气质，而不是只写“克制”或“热血”这种空标签时读

## 使用纪律

1. 先按 `SKILL.md` 主流程完成结构判断
2. 再按任务症状命中一个高级模板
3. 一次最多补读 1-2 份，不要把高阶模板当新的大杂烩

这份导航的作用只有一个：**让高级模板可找、可用，但不反客为主。**
