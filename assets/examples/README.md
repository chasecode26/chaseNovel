# examples assets

这里存放样本与示例型资产。

当前默认做法：

- 从 `docs/assets/genre-index.md` 进入题材导航
- 需要具体题材资料时，优先先读对应 `docs/assets/*-reference-map.md`
- 只按需读取示例，不把整包样本一次性灌入主链

旧样本资料仍有一部分保留在 `references/` 作为兼容资料层，但不再作为默认入口。
